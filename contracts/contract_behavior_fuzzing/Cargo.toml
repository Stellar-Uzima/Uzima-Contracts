[package]
name = "contract_behavior_fuzzing"
version = "0.1.0"
edition = "2021"
authors = ["Your Name <your.email@example.com>"]
license = "MIT"
repository = "https://github.com/yourusername/soroban-project"

[lib]
doctest = false

[dependencies]

[dev-dependencies]
identity_registry = { workspace = true }
medical_records = { workspace = true }
proptest = "1.6.0"
soroban-sdk = { workspace = true, features = ["testutils"] }
sut_token = { workspace = true }
token_sale = { workspace = true }

[features]
default = []
testutils = ["soroban-sdk/testutils"]