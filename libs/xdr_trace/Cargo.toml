[package]
name = "xdr_trace"
version = "0.1.0"
edition.workspace = true

[dependencies]
soroban-sdk = { workspace = true }

[lib]
crate-type = ["rlib"]

[dev-dependencies]
soroban-sdk = { workspace = true }