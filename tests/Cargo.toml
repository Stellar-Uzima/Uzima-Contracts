[package]
name = "uzima-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
soroban-sdk = { workspace = true }
medical_records = { path = "../contracts/medical_records", features = ["testutils"] }
sut_token = { path = "../contracts/sut_token", features = ["testutils"] }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }

[lib]
name = "uzima_tests"
path = "lib.rs"
